# GATE_3_PREFLIGHT_AUDIT.md

## Purpose

This document records the repository-side Gate 3 preflight audit for the GaggiMate-hosted GaggiGo PWA architecture.

The audit was performed before target-device runtime validation to separate proven build/deployment facts from remaining runtime unknowns.

---

## Status

```text
COMPLETE
```

Result:

```text
PASS
```

The repository proves that the intended deployment architecture is not external hosting.

The application build is packaged into the GaggiMate filesystem image and served by GaggiMate itself.

---

## Validated Architecture

The validated architecture is:

```text
GaggiMate ESP32
= firmware runtime
= API authority
= Web UI host
= SPIFFS filesystem host

GaggiGo layer
= embedded frontend/PWA layer
= cache-first observer layer
= IndexedDB mirror
= offline History / Analyzer / Statistics / Archive layer
```

Normal users should not need to:

```text
visit GitHub
install from GitHub Pages
install a second app manually
keep a PC running
understand the build pipeline
```

The PC is a development/build tool only.

It is not part of the runtime architecture.

---

## Proven Build Chain

Repository audit confirmed the following build chain:

```text
web source
↓
Vite build
↓
web/dist
↓
scripts/build_spiffs.sh
↓
data/w
↓
PlatformIO buildfs
↓
.pio/build/display/spiffs.bin
↓
out/display-filesystem.bin
↓
flashed GaggiMate ESP32 filesystem
↓
SPIFFS:/w
↓
WebUIPlugin serves the app shell
```

---

## Evidence: scripts/build_spiffs.sh

The script performs the frontend-to-filesystem copy:

```bash
rm -rf data/w
mkdir -p data/w
mkdir -p data/p

cd web || exit
npm ci
npm run build

cp -R dist/* ../data/w/
gzip ../data/w/assets/*.js
gzip ../data/w/assets/*.css
gzip ../data/w/*.html
```

This proves that the production frontend build is copied into:

```text
data/w
```

which becomes the web app folder in the firmware filesystem image.

---

## Evidence: GitHub Build Workflow

The release workflow performs:

```text
Build Web
→ ./scripts/build_spiffs.sh

Build display FS
→ pio run -t buildfs -e display
→ cp .pio/build/display/spiffs.bin out/display-filesystem.bin
```

This proves that the web app is included in the display filesystem artifact distributed with firmware releases.

---

## Evidence: PlatformIO / SPIFFS

`platformio.ini` includes SPIFFS as a filesystem dependency for display firmware environments.

The display build produces a SPIFFS filesystem image using PlatformIO `buildfs`.

---

## Evidence: WebUIPlugin Serving Path

`src/display/plugins/WebUIPlugin.cpp` serves the web app from SPIFFS:

```cpp
server.serveStatic("/", SPIFFS, "/w")
  .setDefaultFile("index.html")
  .setCacheControl("max-age=0");
```

It also falls back to the app shell for browser routes:

```cpp
server.onNotFound([](AsyncWebServerRequest *request) {
  request->send(SPIFFS, "/w/index.html");
});
```

This proves that the app is intended to be served by GaggiMate, not by GitHub Pages, Cloudflare Pages, Netlify, Vercel, or a PC dev server.

---

## Architecture Decision From Audit

The selected architecture remains:

```text
GaggiMate-hosted single app
with embedded GaggiGo offline observer layer
```

The following models are not required for the intended product path:

```text
GitHub Pages as normal user install path
Cloud-hosted static PWA as normal user install path
PC-hosted dev server as runtime path
second standalone GaggiGo install journey
```

These may remain useful for development/testing only, but they are not the primary architecture.

---

## What This Audit Proves

Proven:

```text
web/dist is generated by the frontend build
web/dist is copied to data/w
data/w is included in the PlatformIO SPIFFS build
SPIFFS image is exported as display-filesystem.bin
WebUIPlugin serves SPIFFS:/w as the web UI
GaggiMate is the runtime web host
PC is not required after firmware/filesystem deployment
GitHub is not required at runtime
external static hosting is not required for the intended user journey
```

---

## What This Audit Does Not Prove

Still unproven:

```text
/w/sw.js is present on the flashed device
/w/workbox-*.js is present on the flashed device
/w/app.webmanifest is present on the flashed device
service worker registers from the GaggiMate-served origin
service worker controls the installed page
home-screen launch survives GaggiMate unavailability
IndexedDB survives target-device offline relaunch
History renders after installed offline relaunch
Analyzer renders after installed offline relaunch
Statistics renders after installed offline relaunch
Archive page works after installed offline relaunch
unsafe live machine controls remain unavailable offline
```

These remain target-device validation items.

---

## Gate Movement

Gate 3 preflight architecture audit is complete.

Gate 3 runtime validation remains open.

Next validation path:

```text
Gate 3A — Static Asset Audit on running GaggiMate
Gate 3B — Static Serving Header Audit
Gate 3C — Service Worker Registration Audit
Gate 3D — Offline App Shell Relaunch
Gate 3E — IndexedDB Offline Data Validation
Gate 3F — Machine-Control Boundary Validation
Gate 3G — SPA Fallback Review
Gate 3H — Packaging / Filesystem Runtime Confirmation
```

---

## Governance Decision

Feature development remains blocked.

Safe Sync remains blocked.

Merge-back remains blocked.

Reason:

```text
The deployment architecture is now proven at repository/build-chain level,
but target-device runtime PWA validation has not yet passed.
```

---

## Current Next Action

When physically back with the GaggiMate device, execute Gate 3A by loading:

```text
http://gaggimate.local/
http://gaggimate.local/sw.js
http://gaggimate.local/app.webmanifest
http://gaggimate.local/assets/
```

Repeat using the device IP address if mDNS fails.

Record pass/fail before patching application logic.
