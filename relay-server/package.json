{
  "name": "gaggimate-relay",
  "version": "1.0.0",
  "description": "WebSocket relay for remote GaggiMate access",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "cf:dev": "wrangler dev",
    "cf:deploy": "wrangler deploy"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "wrangler": "^4.90.0"
  },
  "engines": {
    "node": ">=18"
  }
}
